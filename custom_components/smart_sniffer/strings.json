{
  "config": {
    "step": {
      "user": {
        "title": "Connect to SMART Sniffer Agent",
        "description": "Enter the connection details for the SMART Sniffer agent running on the target machine.",
        "data": {
          "host": "Host",
          "port": "Port",
          "token": "Bearer Token (optional)",
          "scan_interval": "Polling Interval (seconds)"
        },
        "data_description": {
          "host": "IP address or hostname of the machine running the SMART Sniffer agent.",
          "port": "HTTP port the agent is listening on (default 9099).",
          "token": "Leave blank if the agent has no auth configured.",
          "scan_interval": "How often to poll the agent for updated SMART data."
        }
      },
      "zeroconf_confirm": {
        "title": "SMART Sniffer Agent Discovered",
        "description": "Found agent on **{hostname}** ({host}:{port}) with {drives} drive(s).{agent_version_warning}",
        "data": {
          "token": "Bearer Token"
        },
        "data_description": {
          "token": "Enter the bearer token if auth is enabled on this agent. Leave blank otherwise."
        }
      }
    },
    "error": {
      "cannot_connect": "Unable to connect to the SMART Sniffer agent. Check the host, port, and token.",
      "unknown": "An unexpected error occurred."
    },
    "abort": {
      "already_configured": "This SMART Sniffer agent is already configured."
    },
    "flow_title": "SMART Sniffer ({hostname})"
  },
  "options": {
    "step": {
      "init": {
        "title": "SMART Sniffer",
        "menu_options": {
          "settings": "Connection and polling settings",
          "thresholds": "Alert thresholds"
        }
      },
      "settings": {
        "title": "SMART Sniffer Settings",
        "description": "Adjust connection and polling settings. The host cannot be changed — remove and re-add the integration to change it.",
        "data": {
          "port": "Port",
          "token": "Bearer Token",
          "scan_interval": "Polling Interval (seconds)",
          "force_update": "Write a value on every poll"
        },
        "data_description": {
          "port": "HTTP port the agent is listening on.",
          "token": "Leave blank if the agent has no auth configured.",
          "scan_interval": "How often to poll for updated SMART data (in seconds).",
          "force_update": "Enable for InfluxDB, Prometheus, or Grafana. Records a data point on every poll even when the value has not changed. Leave off otherwise to keep the recorder database smaller."
        }
      },
      "thresholds": {
        "title": "Choose a drive",
        "description": "Alert thresholds are set per drive.",
        "data": {
          "drive_id": "Drive"
        }
      },
      "drive_thresholds": {
        "title": "Alert thresholds",
        "description": "Thresholds for {drive}.\n\nSetting a threshold tells SMART Sniffer to treat the current damage as known and alert you only if it gets worse. It does not repair anything, and a drive with reallocated sectors is statistically more likely to fail than one without.\n\nCurrent readings: {readings}\n\nSMART overall status, the NVMe critical warning flag, and a drive reporting that it has reached its own spare threshold are never affected by these settings.",
        "data": {
          "accept_current": "Accept all current readings as the baseline"
        },
        "data_description": {
          "accept_current": "Overrides anything typed below. Sets each threshold to what the drive reports right now, so today's readings stop alerting and anything worse still does."
        }
      }
    },
    "error": {
      "cannot_connect": "Unable to connect with the new settings. Check the port and token.",
      "unknown": "An unexpected error occurred."
    },
    "abort": {
      "no_drives": "The agent reported no readable drives, so there is nothing to set thresholds on. Check that the agent is reachable and try again."
    }
  },
  "services": {
    "get_drive_data": {
      "name": "Get Drive Data",
      "description": "Returns the full cached SMART data for all drives monitored by a SMART Sniffer agent. Useful for passing raw drive health data to an AI for analysis.",
      "fields": {
        "config_entry_id": {
          "name": "Config Entry",
          "description": "The SMART Sniffer agent to query."
        }
      }
    }
  },
  "issues": {
    "agent_outdated": {
      "title": "SMART Sniffer: Update agent on {hostname} (v{current_version} → v{min_version})",
      "description": "The agent on **{hostname}** is running version **{current_version}**, but this integration requires at least **{min_version}**.\n\nTo update, SSH into the host and run:\n```\ncurl -sSL https://raw.githubusercontent.com/DAB-LABS/smart-sniffer/main/install.sh | sudo bash\n```\nThe installer will detect the existing installation and upgrade in place. This repair will clear automatically once the agent reports a compatible version."
    }
  }
}
